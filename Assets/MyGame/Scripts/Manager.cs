using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Manager : MonoBehaviour
{
    // Start is called before the first frame update

    //array von wörtern 
    //Methode die bei drücken von Start button ausgeführt wird und randome einen der begriffe auswählt
    // alphabetische buttons zum buchstaben probieren
    // methode die mit abfragt ob der richtige buchstabe gedrückt wurde
    // Methode die aufgerufen wird wenn ein falscher buchstabe gedrückt wurde, galgen zeichnen
    // methode die aufgerufen wird wenn ein richtiger Buchstabe gedrückt wurde, buchstaben eintragen
    // Verloren Methode und Gewonnen Methode
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
